# 🚀 Быстрая инструкция по деплою

## Шаг 1: Обновить GitHub (в Replit Shell)

Скопируйте и выполните эти команды **в Shell Replit**:

```bash
# Удалить блокировку Git
rm -f .git/index.lock

# Добавить все изменения
git add .

# Создать коммит
git commit -m "Production ready: 7 tour blocks, RU/EN only, filters working"

# Отправить на GitHub
git push origin main
```

Если спросит логин/пароль - используйте GitHub Personal Access Token вместо пароля.

---

## Шаг 2: Обновить сайт на вашем сервере

### Способ А: Через скрипт (САМЫЙ ПРОСТОЙ)

Подключитесь к серверу и выполните:

```bash
cd /var/www/bunyod-tour
./update.sh
```

### Способ Б: Вручную

```bash
cd /var/www/bunyod-tour
git pull origin main
npm install
npx prisma generate
npx prisma db push
pm2 restart all
```

---

## Шаг 3: Проверка

Откройте в браузере:
- `https://ваш_домен.com` - главная страница
- `https://ваш_домен.com/tours-search.html` - страница поиска с 7 блоками
- `https://ваш_домен.com/admin-dashboard.html` - админ панель

---

## 🆘 Если что-то не работает

### Проблема: Git push не работает
```bash
# Проверьте подключение
git remote -v

# Переподключите репозиторий
git remote remove origin
git remote add origin https://github.com/greyversarex/bunyod-tour-production.git
```

### Проблема: Сервер не обновляется
```bash
# Проверьте статус PM2
pm2 status

# Посмотрите логи
pm2 logs bunyod-tour --lines 50
```

### Проблема: База данных не обновилась
```bash
# Принудительное обновление схемы
npx prisma db push --accept-data-loss

# Проверка данных
npx prisma studio
```

---

## 📁 Важные файлы

- **DEPLOYMENT_GUIDE.md** - полная инструкция по развертыванию с нуля
- **update.sh** - скрипт автообновления для сервера
- **ecosystem.config.js** - конфигурация PM2
- **replit.md** - техническая документация проекта

---

## ✅ Что было обновлено (Oct 10, 2025)

1. ✅ **7 блоков туров** - точно 7 "железобетонных" блоков (Популярные, Комбинированные, + 5 стран)
2. ✅ **Таджикский язык удалён** - только RU/EN в категориях и блоках
3. ✅ **Фильтр блоков** - полностью рабочий на странице поиска
4. ✅ **Breadcrumb навигация** - клик на блок → переход на поиск с фильтром
5. ✅ **Программа тура** - удалён fallback контент
6. ✅ **Валюта TJS** - отображается только символ "с."
7. ✅ **База данных** - удалены 4 лишних блока (было 11, стало 7)

**Платформа готова к production!** 🎉
