У меня в проекте есть админ-панель (файл frontend/admin-dashboard.html), через которую я создаю и редактирую туры и отели.  

Проблема: когда нажимаю кнопку «Редактировать» — форма открывается, но большинство полей (текстовые, select, checkbox, изображения и т.д.), которые я заполнял при создании, не подставляются обратно. Из-за этого после сохранения часть старых данных теряется или создаётся новая запись вместо обновления.

Требуется: провести **полное, пошаговое расследование** и дать **100 %-ное решение**.

1. Проверь, как вызывается модальное окно/форма при редактировании:
   • какая функция открывает форму (`editTour`, `editHotel` и т.д.);  
   • действительно ли она делает GET запрос (`/api/tours/:id`, `/api/hotels/:id`) к серверу;  
   • какие данные приходят в ответ (покажи пример JSON).

2. Проанализируй JS-код, который должен заполнять форму:
   • правильно ли значения из JSON присваиваются `input.value` / `select.value`;  
   • обрабатываются ли многоязычные поля `{ ru, en, tj }`;  
   • есть ли скрытое поле `<input type="hidden" id="...-id">`, куда пишется ID записи.

3. Убедись, что при нажатии «Сохранить»:
   • если у объекта есть ID – уходит `PUT /api/.../:id`;  
   • если ID пустой – `POST /api/...` (создание);  
   • что именно уходит в `body` запроса – все ли поля, особенно изображения, чекбоксы и select.

4. Проверь маршруты backend (`src/routes/*Routes.ts`) и контроллеры (`tourController.ts`, `hotelController.ts`):
   • есть ли `GET /api/.../:id` и `PUT /api/.../:id`;  
   • не отфильтровываются ли поля в контроллере перед отправкой в базу.

5. Дай точный список найденных ошибок (frontend / backend) и покажи **готовые правки**:
   • HTML-форма – какие поля/атрибуты добавить;  
   • JavaScript – новая функция `loadForEdit(id)` и обновлённый `save()` с POST/PUT;  
   • при необходимости – изменения в маршрутах или контроллерах.

В ответ:
• пошаговый отчёт «как есть → как должно быть»;  
• исправленный фрагмент HTML + JS (можно вставить сразу в admin-dashboard.html);  
• при необходимости – исправленный маршрут или контроллер (TypeScript).

Не прыгай сразу к коду, сначала покажи анализ, потом предложи изменения. Ответ пиши на русском.