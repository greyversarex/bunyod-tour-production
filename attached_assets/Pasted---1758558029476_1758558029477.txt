Теперь нам нужно реализовать две критически важные функции, которых не хватает в админ-панели.

Твоя задача — работать СТРОГО ПОСЛЕДОВАТЕЛЬНО. Не приступай ко второй части, пока полностью не закончишь и не подтвердишь выполнение первой. Используй существующий раздел "Туры" как образец для дизайна и логики интерфейсов.

---

**Часть 1: Модерация Отзывов (Приоритет №1)**

**Задача:** Создать в админ-панели интерфейс для модерации отзывов.

**Шаг 1: Анализ.**
*   Изучи модель `Review` в `schema.prisma`. Убедись, что в ней есть поле для статуса (например, `status` со значениями `PENDING`, `APPROVED`, `REJECTED`). Если такого поля нет, добавь его и подготовь миграцию.
*   Изучи `reviewController.ts`, чтобы понять, какие роуты уже существуют.

**Шаг 2: План.**
Прежде чем писать код, предоставь мне план. Он должен включать:
1.  **Интерфейс:** "Я создам в админ-панели новый раздел 'Модерация Отзывов'. В нем будет таблица со всеми отзывами, у которых статус `PENDING`."
2.  **Элементы управления:** "В каждой строке таблицы будут данные отзыва и две кнопки: 'Одобрить' и 'Отклонить'."
3.  **Логика на бэкенде:** "Я создам в `reviewController.ts` два новых PATCH-роута: `/api/reviews/:id/approve` и `/api/reviews/:id/reject`. Первый будет менять статус отзыва на `APPROVED`, второй — на `REJECTED`."

**Шаг 3: Выполнение.**
После моего одобрения плана, приступай к его реализации.

**Шаг 4: Подтверждение.**
В конце подтверди: "Часть 1 завершена. Интерфейс модерации отзывов полностью реализован и готов к работе."

---

**Часть 2: Управление Категориями (Приоритет №2)**

**ВАЖНО:** Приступай к этой части ТОЛЬКО ПОСЛЕ УСПЕШНОГО ЗАВЕРШЕНИЯ Части 1.

**Задача:** Создать в админ-панели полноценный интерфейс для управления категориями туров.

**Шаг 1: Анализ.**
*   Ты уже подтверждал, что бэкенд для категорий (`categoryController.ts`) полностью готов. Проверь его еще раз.
*   Изучи, как реализован интерфейс для "Туров", чтобы сделать категории в том же стиле.

**Шаг 2: План.**
Предоставь мне план:
1.  **Интерфейс:** "Я создам в админ-панели новый раздел 'Категории'. В нем будет таблица со списком всех существующих категорий и кнопка 'Создать новую категорию'."
2.  **Функционал:** "Я реализую полный CRUD:
    *   **Создание:** По кнопке будет открываться форма для ввода названия категории (на RU/EN).
    *   **Редактирование:** В каждой строке таблицы будет кнопка 'Редактировать', открывающая ту же форму для изменения названия.
    *   **Удаление:** В каждой строке будет кнопка 'Удалить' с подтверждением действия."

**Ша- 3: Выполнение.**
После моего одобрения плана, приступай к его реализации.

**Шаг 4: Подтверждение.**
В конце подтверди: "Часть 2 завершена. Интерфейс управления категориями полностью реализован."