# ===========================================
# BUNYOD-TOUR VDS PRODUCTION CONFIGURATION
# ===========================================
# Скопируйте этот файл как .env на VDS сервере
# Заполните все значения перед запуском

# ===========================================
# ОБЯЗАТЕЛЬНЫЕ ПЕРЕМЕННЫЕ (КРИТИЧНО!)
# ===========================================

# Режим работы (обязательно production для VDS)
NODE_ENV=production

# Подключение к PostgreSQL базе данных Timeweb
# Формат: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
# Получите эти данные из панели управления Timeweb
DATABASE_URL=postgresql://username:password@localhost:5432/bunyod_tour?schema=public

# Секретный ключ для JWT токенов (минимум 32 символа!)
# Сгенерируйте уникальный ключ: openssl rand -base64 48
JWT_SECRET=your-super-secret-jwt-key-min-32-chars-change-this-value

# ===========================================
# ДОМЕНЫ И CORS
# ===========================================

# Основной домен вашего сайта (без https://)
PRODUCTION_DOMAIN=bunyodtour.tj

# Разрешенные CORS домены (через запятую)
ALLOWED_ORIGINS=https://bunyodtour.tj,https://www.bunyodtour.tj

# Базовый URL API сервера (для callback платежных систем)
BASE_URL=https://bunyodtour.tj

# URL фронтенда (для ссылок в письмах и перенаправлений)
FRONTEND_URL=https://bunyodtour.tj

# Порт сервера (по умолчанию 5000)
PORT=5000

# ===========================================
# EMAIL СЕРВИС (для уведомлений)
# ===========================================

SMTP_HOST=mail.timeweb.com
SMTP_PORT=465
SMTP_USER=booking@bunyodtour.tj
SMTP_PASS=your-email-password
SMTP_FROM=booking@bunyodtour.tj

# ===========================================
# ПЛАТЕЖНЫЕ СИСТЕМЫ
# ===========================================

# AlifPay (Таджикистан)
ALIF_MERCHANT_KEY=your-alif-merchant-key
ALIF_MERCHANT_PASSWORD=your-alif-merchant-password
ALIF_API_URL=https://api.alif.tj

# Stripe (международные платежи)
STRIPE_SECRET_KEY=sk_live_your-stripe-key
STRIPE_PUBLISHABLE_KEY=pk_live_your-stripe-key

# Payme (Узбекистан)
PAYME_MERCHANT_ID=your-payme-merchant-id
PAYME_SECRET_KEY=your-payme-secret-key

# Click (Узбекистан)
CLICK_MERCHANT_ID=your-click-merchant-id
CLICK_SECRET_KEY=your-click-secret-key

# ===========================================
# OBJECT STORAGE
# ===========================================

# Внешнее объектное хранилище (если используется)
STORAGE_SIDECAR_ENDPOINT=http://127.0.0.1:1106
STORAGE_AUDIENCE=object-storage

# ===========================================
# БЕЗОПАСНОСТЬ
# ===========================================

# Google reCAPTCHA (защита от ботов)
RECAPTCHA_SECRET_KEY=your-recaptcha-secret-key

# Session Secret (для сессий)
SESSION_SECRET=your-session-secret-min-32-chars

# ===========================================
# API КЛЮЧИ СТОРОННИХ СЕРВИСОВ
# ===========================================

# Google Maps API (для карт)
GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# SMS сервис (если используется)
SMS_API_KEY=your-sms-api-key
SMS_API_URL=https://sms-provider.com/api

# ===========================================
# ЛОГИРОВАНИЕ И МОНИТОРИНГ
# ===========================================

# Уровень логов (error, warn, info, debug)
LOG_LEVEL=info

# Sentry для отслеживания ошибок (опционально)
SENTRY_DSN=your-sentry-dsn

# ===========================================
# ДОПОЛНИТЕЛЬНЫЕ НАСТРОЙКИ
# ===========================================

# Максимальный размер загружаемых файлов (в МБ)
MAX_FILE_SIZE=10

# Таймаут запросов (в мс)
REQUEST_TIMEOUT=30000

# ===========================================
# ПРИМЕЧАНИЯ ДЛЯ МИГРАЦИИ:
# ===========================================
# 1. DATABASE_URL должен указывать на существующую БД Timeweb
# 2. JWT_SECRET должен быть уникальным (не копируйте из примера!)
# 3. NODE_ENV=production критично для безопасной работы
# 4. Все секреты храните в безопасности, не коммитьте в Git
# ===========================================
