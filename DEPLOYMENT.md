# 🚀 Руководство по миграции Bunyod-Tour на Timeweb VDS

## 📋 Оглавление
1. [Подготовка к миграции](#подготовка-к-миграции)
2. [Настройка VDS сервера](#настройка-vds-сервера)
3. [Развертывание приложения](#развертывание-приложения)
4. [Настройка Nginx](#настройка-nginx)
5. [Финальная проверка](#финальная-проверка)
6. [Устранение проблем](#устранение-проблем)

---

## 🎯 Подготовка к миграции

### 1. Сохранение всех изменений на Replit

**ВАЖНО**: Перед миграцией убедитесь, что все изменения сохранены!

```bash
# На Replit выполните:
git status                    # Проверьте изменения
git add .                     # Добавьте все файлы
git commit -m "Production ready version with all improvements"
git push origin main         # Загрузите на GitHub
```

### 2. Резервное копирование базы данных Timeweb

**КРИТИЧНО**: Сделайте бэкап существующей БД перед миграцией!

```bash
# На VDS сервере выполните:
pg_dump -U postgres -d bunyod_tour > backup_$(date +%Y%m%d_%H%M%S).sql
```

### 3. Проверка совместимости схемы

Текущая версия сайта использует улучшенную схему БД. Убедитесь, что:
- Существуют все необходимые таблицы
- Структура таблиц совместима с новой версией

---

## 🖥️ Настройка VDS сервера

### 1. Подключение к серверу

```bash
ssh root@your-vds-ip
# или
ssh your-username@your-vds-ip
```

### 2. Установка необходимого ПО

```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка Node.js 20 (LTS)
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs

# Проверка версий
node --version    # должно быть v20.x.x
npm --version     # должно быть v10.x.x

# Установка PM2 (менеджер процессов)
sudo npm install -g pm2

# Установка Nginx
sudo apt install -y nginx

# Установка Git
sudo apt install -y git
```

### 3. Настройка PostgreSQL (если еще не настроен)

```bash
# Проверка статуса PostgreSQL
sudo systemctl status postgresql

# Если нужно установить:
sudo apt install -y postgresql postgresql-contrib

# Создание базы данных (если нужно)
sudo -u postgres psql
CREATE DATABASE bunyod_tour;
CREATE USER bunyod_user WITH PASSWORD 'your-secure-password';
GRANT ALL PRIVILEGES ON DATABASE bunyod_tour TO bunyod_user;
\q
```

---

## 📦 Развертывание приложения

### 1. Клонирование репозитория

```bash
# Перейдите в директорию проектов
cd /var/www

# Клонируйте репозиторий с GitHub
sudo git clone https://github.com/your-username/bunyod-tour.git
cd bunyod-tour

# Дайте права текущему пользователю
sudo chown -R $USER:$USER /var/www/bunyod-tour
```

### 2. Настройка переменных окружения

```bash
# Скопируйте шаблон
cp .env.production.template .env

# Отредактируйте .env файл
nano .env
```

**Обязательно настройте**:

```env
# КРИТИЧНО!
NODE_ENV=production
DATABASE_URL=postgresql://bunyod_user:your-password@localhost:5432/bunyod_tour?schema=public
JWT_SECRET=your-unique-secret-min-32-chars

# Ваш домен
PRODUCTION_DOMAIN=bunyod-tour.com
ALLOWED_ORIGINS=https://bunyod-tour.com,https://www.bunyod-tour.com

# Остальные переменные согласно вашей конфигурации
```

**Генерация безопасного JWT_SECRET**:
```bash
openssl rand -base64 48
```

### 3. Установка зависимостей

```bash
# Установка пакетов
npm run prod:setup

# Это выполнит:
# - npm install
# - prisma generate
```

### 4. Применение миграций базы данных

**ВНИМАНИЕ**: Этот шаг критичен для сохранности данных!

```bash
# Проверка подключения к БД
npx prisma db pull

# Применение миграций (безопасно)
npm run prod:migrate

# Это выполнит: NODE_ENV=production prisma migrate deploy
```

**Если миграции отсутствуют** (используется только schema.prisma):

```bash
# Создайте начальную миграцию
npx prisma migrate dev --name init --create-only

# Пометьте как примененную (без изменений в БД)
npx prisma migrate resolve --applied init

# Теперь можно применять будущие миграции
npm run prod:migrate
```

### 5. Создание необходимых директорий

```bash
# Создание директорий для загрузок
mkdir -p uploads/images
mkdir -p uploads/guides
mkdir -p uploads/hotels

# Установка прав
chmod 755 uploads
chmod 755 uploads/images
chmod 755 uploads/guides
chmod 755 uploads/hotels
```

### 6. Запуск приложения с PM2

```bash
# Запуск приложения
npm run prod:start

# Сохранение конфигурации PM2
pm2 save

# Автозапуск при перезагрузке сервера
pm2 startup
# Выполните команду, которую выведет PM2

# Проверка статуса
npm run prod:status
```

**Полезные команды PM2**:
```bash
npm run prod:logs      # Просмотр логов
npm run prod:restart   # Перезапуск приложения
npm run prod:stop      # Остановка приложения
pm2 monit              # Мониторинг в реальном времени
```

---

## 🌐 Настройка Nginx

### 1. Создание конфигурации Nginx

```bash
sudo nano /etc/nginx/sites-available/bunyod-tour
```

**Вставьте следующую конфигурацию**:

```nginx
server {
    listen 80;
    server_name bunyod-tour.com www.bunyod-tour.com;

    # Увеличенный размер загрузки файлов
    client_max_body_size 50M;

    # Логи
    access_log /var/log/nginx/bunyod-tour-access.log;
    error_log /var/log/nginx/bunyod-tour-error.log;

    # Прокси к Node.js приложению
    location / {
        proxy_pass http://localhost:5000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # Таймауты
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # Статические файлы (опционально для кэширования)
    location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2)$ {
        proxy_pass http://localhost:5000;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

### 2. Активация конфигурации

```bash
# Создание символической ссылки
sudo ln -s /etc/nginx/sites-available/bunyod-tour /etc/nginx/sites-enabled/

# Удаление дефолтной конфигурации (если есть)
sudo rm /etc/nginx/sites-enabled/default

# Проверка конфигурации
sudo nginx -t

# Перезагрузка Nginx
sudo systemctl reload nginx
```

### 3. Настройка SSL (HTTPS)

```bash
# Установка Certbot
sudo apt install -y certbot python3-certbot-nginx

# Получение SSL сертификата
sudo certbot --nginx -d bunyod-tour.com -d www.bunyod-tour.com

# Автообновление сертификата
sudo certbot renew --dry-run
```

---

## ✅ Финальная проверка

### 1. Проверка работоспособности

```bash
# Проверка статуса приложения
pm2 status

# Проверка логов
pm2 logs bunyod-tour --lines 50

# Проверка Nginx
sudo systemctl status nginx

# Проверка подключения к БД
sudo -u postgres psql -d bunyod_tour -c "SELECT COUNT(*) FROM \"Tour\";"
```

### 2. Тестирование сайта

Откройте в браузере:
- http://your-domain.com (должен перенаправить на HTTPS)
- https://your-domain.com

**Проверьте**:
- ✅ Главная страница загружается
- ✅ Туры отображаются корректно
- ✅ Изображения загружаются
- ✅ Админ панель доступна (https://your-domain.com/admin-dashboard.html)
- ✅ Личный кабинет гида работает
- ✅ Бронирование функционирует
- ✅ Все улучшения сохранены

### 3. Мониторинг

```bash
# Установка мониторинга ошибок
pm2 install pm2-logrotate

# Настройка ротации логов
pm2 set pm2-logrotate:max_size 10M
pm2 set pm2-logrotate:retain 30
```

---

## 🔧 Устранение проблем

### Проблема: Приложение не запускается

```bash
# Проверьте логи
pm2 logs bunyod-tour --err

# Проверьте переменные окружения
cat .env | grep -v "SECRET\|PASSWORD"

# Проверьте подключение к БД
node -e "require('dotenv').config(); console.log(process.env.DATABASE_URL)"
```

### Проблема: Ошибки миграции

```bash
# Сброс схемы БД (ОСТОРОЖНО!)
npx prisma db push --force-reset  # ⚠️ Удалит все данные!

# Или создайте миграцию вручную
npx prisma migrate dev --name fix_schema
```

### Проблема: Nginx 502 Bad Gateway

```bash
# Проверьте, что приложение запущено
pm2 status

# Проверьте порт
netstat -tulpn | grep 5000

# Проверьте логи Nginx
sudo tail -f /var/log/nginx/bunyod-tour-error.log
```

### Проблема: CORS ошибки

Убедитесь, что в `.env` файле:
```env
PRODUCTION_DOMAIN=bunyod-tour.com
ALLOWED_ORIGINS=https://bunyod-tour.com,https://www.bunyod-tour.com
```

---

## 🔄 Обновление приложения

После внесения изменений на Replit:

```bash
# На VDS сервере
cd /var/www/bunyod-tour

# Получение обновлений
git pull origin main

# Установка новых зависимостей
npm install

# Применение миграций
npm run prod:migrate

# Перезапуск приложения
npm run prod:restart

# Проверка статуса
npm run prod:logs
```

---

## 📝 Важные заметки

### ✅ Что сохранено
- Все улучшения дизайна и функциональности
- Корпоративные цвета в личном кабинете гида
- Исправления авторизации и безопасности
- Многоязычная система
- Продвинутый поиск
- Все модули бронирования

### ⚠️ Критично
1. **NODE_ENV=production** обязательно должен быть установлен
2. **DATABASE_URL** должен указывать на базу Timeweb
3. **JWT_SECRET** должен быть уникальным (не из примера!)
4. Сделайте **резервную копию БД** перед миграцией

### 🔒 Безопасность
- Файл `.env` не должен быть в Git
- Все секреты храните в безопасности
- Регулярно обновляйте зависимости
- Настройте firewall на VDS

---

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи: `pm2 logs bunyod-tour`
2. Проверьте статус: `pm2 status`
3. Проверьте Nginx: `sudo nginx -t`
4. Проверьте БД: подключение и данные

---

**Успешной миграции! 🚀**
