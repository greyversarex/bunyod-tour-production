# 🚀 Инструкция по "Тотальной Перезагрузке" на TimeWeb

Эта инструкция поможет вам развернуть проект Bunyod-Tour на чистый VDS сервер TimeWeb с нуля.

## 📋 Оглавление
1. [Предварительные требования](#-предварительные-требования)
2. [Этап 1: Подготовка (Остановка и Очистка)](#-этап-1-подготовка-остановка-и-очистка)
3. [Этап 2: Установка](#-этап-2-установка)
4. [Этап 3: Настройка](#-этап-3-настройка)
5. [Этап 4: Первый запуск и инициализация БД](#-этап-4-первый-запуск-и-инициализация-бд)
6. [Этап 5: Финальный запуск](#-этап-5-финальный-запуск)
7. [Проверка работы](#-проверка-работы)
8. [Обновление сайта в будущем](#-обновление-сайта-в-будущем)
9. [Устранение проблем](#-устранение-проблем)

---

## 📋 Предварительные требования

На сервере должно быть установлено:
- **Node.js** (версия 18 или выше)
- **PostgreSQL** (версия 14 или выше)
- **PM2** (менеджер процессов)
- **Git**

---

## 🔧 Этап 1: Подготовка (Остановка и Очистка)

### 1.1 Подключитесь к серверу по SSH
```bash
ssh root@your-server-ip
```

### 1.2 Остановите старый сайт (если запущен)
```bash
pm2 stop all
pm2 delete all
```

### 1.3 Удалите старую папку проекта
```bash
cd /var/www  # или где у вас находится проект
rm -rf bunyod-tour  # Замените на имя вашей папки
```

### 1.4 Полностью удалите и пересоздайте базу данных
```bash
# Подключитесь к PostgreSQL
sudo -u postgres psql

# В консоли PostgreSQL выполните:
DROP DATABASE IF EXISTS bunyod_tour;
CREATE DATABASE bunyod_tour;

# Создайте пользователя (если еще не создан)
CREATE USER bunyod_user WITH PASSWORD 'your_secure_password';
GRANT ALL PRIVILEGES ON DATABASE bunyod_tour TO bunyod_user;

# Выйдите из PostgreSQL
\q
```

---

## 📦 Этап 2: Установка

### 2.1 Клонируйте чистый репозиторий с GitHub
```bash
cd /var/www
git clone https://github.com/your-username/bunyod-tour.git
cd bunyod-tour
```

### 2.2 Установите зависимости
```bash
npm install
```

> **Примечание**: Команда `npm install` автоматически выполнит `prisma generate` (см. postinstall в package.json)

---

## ⚙️ Этап 3: Настройка

### 3.1 Создайте файл `.env` на основе `.env.example`
```bash
cp .env.example .env
nano .env  # или используйте vi/vim
```

### 3.2 Заполните обязательные переменные в `.env`:

**МИНИМАЛЬНАЯ КОНФИГУРАЦИЯ ДЛЯ СТАРТА:**
```env
# База данных
DATABASE_URL="postgresql://bunyod_user:your_secure_password@localhost:5432/bunyod_tour"

# Безопасность
JWT_SECRET="ваш-супер-секретный-ключ-смените-это"
NODE_ENV="production"

# Сервер
PORT=5000
PRODUCTION_DOMAIN="your-domain.com"

# Email (опционально, но рекомендуется)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_USER="your-email@gmail.com"
SMTP_PASSWORD="your-app-password"
SMTP_FROM="Bunyod Tour <noreply@bunyod-tour.tj>"
```

> **ВАЖНО**: Обязательно смените `JWT_SECRET` на уникальный случайный ключ!

**Для генерации JWT_SECRET:**
```bash
node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
```

### 3.3 Настройка платежных систем (опционально)

Если вы хотите использовать платежные системы, добавьте в `.env`:

```env
# AlifPay (Таджикистан)
ALIF_MERCHANT_KEY="your-alif-merchant-key"
ALIF_MERCHANT_PASSWORD="your-alif-merchant-password"

# Payler (Россия/СНГ)
PAYLER_MERCHANT_ID="your-payler-merchant-id"
PAYLER_SECRET_KEY="your-payler-secret-key"
```

---

## 🏗️ Этап 4: Первый запуск и инициализация БД

### 4.1 Примените схему базы данных
```bash
npx prisma db push
```

> Эта команда создаст все таблицы в базе данных согласно Prisma схеме.

### 4.2 Заполните базу данных начальными данными (Seeding)
```bash
npx prisma db seed
```

> **Что создаст seed скрипт:**
> - ✅ Администратор: `admin` / `admin12345`
> - ✅ Курсы валют: TJS, USD, EUR, RUB, CNY
> - ✅ Страны: Таджикистан, Узбекистан, Киргизстан, Казахстан, Туркменистан
> - ✅ Города: Душанбе, Худжанд, Самарканд, Бишкек, Алматы и др.
> - ✅ Примеры категорий и туров

---

## 🚀 Этап 5: Финальный запуск

### 5.1 Создайте папку для логов PM2
```bash
mkdir -p logs
```

### 5.2 Запустите приложение через PM2
```bash
pm2 start ecosystem.config.js
```

### 5.3 Сохраните конфигурацию PM2 для автозапуска
```bash
pm2 save
pm2 startup
```

> Система покажет команду для автозапуска - скопируйте и выполните её.

### 5.4 Проверьте статус
```bash
pm2 status
pm2 logs bunyod-tour
```

---

## ✅ Проверка работы

### 1. Откройте сайт в браузере
```
http://your-server-ip:5000
```

### 2. Войдите в админку
```
http://your-server-ip:5000/admin-dashboard.html

Логин: admin
Пароль: admin12345
```

> **ВАЖНО**: Сразу смените пароль администратора после первого входа!

### 3. Проверьте калькулятор валют
- Откройте главную страницу
- Проверьте, что калькулятор отображает валюты: TJS, USD, EUR, RUB, CNY

### 4. Проверьте туры
- Должны отображаться примеры туров из seed данных
- Новый формат программы тура должен работать

---

## 🔄 Обновление сайта в будущем

После первого развертывания, для обновлений используйте:

```bash
cd /var/www/bunyod-tour
git pull origin main  # или master
npm install  # если добавились новые зависимости
npx prisma db push  # если изменилась схема БД
pm2 restart bunyod-tour
```

---

## 🐛 Устранение проблем

### Проблема: Сайт не запускается
```bash
# Проверьте логи
pm2 logs bunyod-tour

# Проверьте, что БД доступна
psql -U bunyod_user -d bunyod_tour -h localhost
```

### Проблема: Калькулятор валют не работает
```bash
# Убедитесь, что seed данные загружены
psql -U bunyod_user -d bunyod_tour -c "SELECT * FROM exchange_rates;"

# Если пусто - запустите seed снова
npx prisma db seed
```

### Проблема: Ошибка "JWT_SECRET is not defined"
```bash
# Проверьте .env файл
cat .env | grep JWT_SECRET

# Добавьте переменную, если её нет
echo 'JWT_SECRET="your-secret-key-here"' >> .env
pm2 restart bunyod-tour
```

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи: `pm2 logs bunyod-tour`
2. Проверьте статус БД: `sudo systemctl status postgresql`
3. Проверьте .env файл: `cat .env`

---

## 🎉 Готово!

После выполнения всех шагов:
- ✅ Сайт работает на порту 5000
- ✅ База данных заполнена начальными данными
- ✅ Админ панель доступна (admin / admin12345)
- ✅ Калькулятор валют работает
- ✅ Платежные системы настроены (если добавлены ключи)
- ✅ PM2 автоматически перезапустит приложение при падении

**Ваш сайт готов к использованию! 🚀**
