# 🚨 ПЛАН ВОССТАНОВЛЕНИЯ ФОТОГРАФИЙ НА PRODUCTION

## 📋 Краткое описание проблемы

**ПРИЧИНА**: При каждом запуске `update.sh` команда `git reset --hard origin/main` удаляет папку `uploads/` со ВСЕМИ загруженными фотографиями туров и отелей, потому что эта папка отслеживалась git'ом.

**РЕЗУЛЬТАТ**: На production сервере пропали все фотографии 40+ туров и отелей. В базе данных остались только пути к файлам (например: `/uploads/images/hotel-123.jpg`), но сами файлы удалены.

**ИСПРАВЛЕНИЕ**: Добавили `/uploads/` в `.gitignore` и создали систему symlink для постоянного хранения файлов.

---

## 🔧 ШАГ 1: ВОССТАНОВЛЕНИЕ ФОТОГРАФИЙ ИЗ БЭКАПА

### Вариант А: Восстановление из файлового бэкапа Timeweb

Если у вас есть файловый snapshot/backup на Timeweb:

```bash
# 1. Подключитесь к production серверу
ssh user@your-production-server

# 2. Проверьте доступные бэкапы Timeweb
# (это зависит от вашего тарифа Timeweb - обычно в панели управления)

# 3. Восстановите папку uploads из бэкапа
# ВАЖНО: Выберите бэкап ДО последнего обновления, когда фото еще были
# Пример (путь зависит от вашей системы бэкапов):
cp -r /path/to/backup/srv/bunyod-tour/uploads /tmp/uploads-backup

# 4. Проверьте что файлы на месте
ls -lh /tmp/uploads-backup/images/
# Должны увидеть файлы типа: hotel-1234567890-123456.jpg
```

### Вариант Б: Если нет файлового бэкапа - восстановление из БД

Если файловых бэкапов нет, нужно будет **ПОВТОРНО ЗАГРУЗИТЬ** все фотографии. Список фотографий можно получить из бэкапа базы данных БЕЗ риска повреждения production БД:

```bash
# 1. Подключитесь к production серверу
ssh user@your-production-server

# 2. Посмотрите список бэкапов БД
ls -lh /var/backups/bunyod-tour/
# Выберите СТАРЫЙ бэкап ДО потери фотографий: bunyod_tour_YYYY-MM-DD_HH-MM-SS.dump

# 3. БЕЗОПАСНО: Создайте ВРЕМЕННУЮ базу данных для анализа бэкапа
sudo -u postgres createdb bunyod_tour_temp

# 4. Восстановите бэкап во ВРЕМЕННУЮ БД (НЕ в production!)
# ВАЖНО: pg_dump создает plain-text SQL, поэтому используем psql:
sudo -u postgres psql -d bunyod_tour_temp -f /var/backups/bunyod-tour/bunyod_tour_YYYY-MM-DD.dump

# 5. Извлеките список всех путей к фотографиям из ВРЕМЕННОЙ БД
# Для туров:
sudo -u postgres psql -d bunyod_tour_temp -c "SELECT id, title->>'ru' as title_ru, main_image, images FROM tours WHERE main_image IS NOT NULL OR images IS NOT NULL;" > /tmp/tours_photos_list.txt

# Для отелей:
sudo -u postgres psql -d bunyod_tour_temp -c "SELECT id, name->>'ru' as name_ru, images FROM hotels WHERE images IS NOT NULL;" > /tmp/hotels_photos_list.txt

# 6. Удалите ВРЕМЕННУЮ БД (больше не нужна)
sudo -u postgres dropdb bunyod_tour_temp

# 7. Теперь у вас есть списки ВСЕХ фотографий которые нужно повторно загрузить
cat /tmp/tours_photos_list.txt
cat /tmp/hotels_photos_list.txt
```

**АЛЬТЕРНАТИВНЫЙ МЕТОД** (извлечение путей напрямую из SQL дампа без создания временной БД):

```bash
# Найти все пути к фотографиям в SQL дампе
grep -E "main_image|images.*uploads" /var/backups/bunyod-tour/bunyod_tour_YYYY-MM-DD.dump | grep -oE "'/uploads/[^']+'" | sort -u > /tmp/all_image_paths.txt

cat /tmp/all_image_paths.txt
# Список ВСЕХ путей к фотографиям из бэкапа
```

**ВНИМАНИЕ**: В этом случае вам придется вручную загрузить фотографии заново через админ-панель для каждого тура/отеля, используя списки выше как справочник.

---

## 🔧 ШАГ 2: СОЗДАНИЕ ПОСТОЯННОГО ХРАНИЛИЩА (НА PRODUCTION СЕРВЕРЕ)

Теперь нужно создать **постоянную директорию** для uploads вне APP_DIR, чтобы `git reset --hard` больше не удалял фотографии.

```bash
# 1. Подключитесь к production серверу
ssh user@your-production-server

# 2. Создайте постоянную директорию для uploads
sudo mkdir -p /var/bunyod-tour/uploads/images
sudo mkdir -p /var/bunyod-tour/uploads/tours
sudo mkdir -p /var/bunyod-tour/uploads/hotels
sudo mkdir -p /var/bunyod-tour/uploads/guides
sudo mkdir -p /var/bunyod-tour/uploads/drivers
sudo mkdir -p /var/bunyod-tour/uploads/slides

# 3. Установите правильные права доступа
# (замените YOUR_USER на пользователя под которым запускается Node.js/PM2)
sudo chown -R YOUR_USER:YOUR_USER /var/bunyod-tour/uploads
sudo chmod -R 755 /var/bunyod-tour/uploads

# 4. Если восстановили файлы из бэкапа - скопируйте их в постоянное хранилище
cp -r /tmp/uploads-backup/* /var/bunyod-tour/uploads/

# 5. Проверьте что файлы на месте
ls -lh /var/bunyod-tour/uploads/images/
```

---

## 🔧 ШАГ 3: СОЗДАНИЕ SYMLINK В APP_DIR

Теперь создадим symlink из `/srv/bunyod-tour/uploads` на постоянную директорию `/var/bunyod-tour/uploads`:

```bash
# 1. Перейдите в директорию приложения
cd /srv/bunyod-tour

# 2. УДАЛИТЕ старую папку uploads (если она есть)
rm -rf uploads

# 3. Создайте symlink
ln -s /var/bunyod-tour/uploads uploads

# 4. Проверьте что symlink создан правильно
ls -la | grep uploads
# Должны увидеть: uploads -> /var/bunyod-tour/uploads

# 5. Проверьте доступ к файлам через symlink
ls -lh uploads/images/
```

---

## 🔧 ШАГ 4: ОБНОВЛЕНИЕ update.sh СКРИПТА

Обновленный файл `update.sh` уже готов в этом коммите. Ключевые изменения:

```bash
# НОВОЕ: Проверка что symlink существует перед деплоем
if [ ! -L "$APP_DIR/uploads" ]; then
  echo "⚠️ КРИТИЧНО: symlink uploads не настроен!"
  echo "Создаю symlink на постоянное хранилище..."
  rm -rf "$APP_DIR/uploads"
  ln -s /var/bunyod-tour/uploads "$APP_DIR/uploads"
fi
```

Это гарантирует, что даже если `git reset --hard` удалит symlink, он будет автоматически восстановлен.

---

## 🔧 ШАГ 5: ОБНОВЛЕНИЕ РЕПОЗИТОРИЯ НА GITHUB

После всех изменений в коде (добавление uploads в .gitignore, обновление update.sh):

```bash
# НА REPLIT (здесь):
git add .gitignore update.sh uploads/.gitkeep RECOVERY_PLAN.md
git commit -m "fix: prevent uploads deletion during deploy + recovery plan"
git push origin main

# НА PRODUCTION СЕРВЕРЕ:
cd /srv/bunyod-tour
./update.sh
```

**ВАЖНО**: При первом запуске `update.sh` после этих изменений, скрипт автоматически создаст symlink если его нет.

---

## ✅ ШАГ 6: ПРОВЕРКА ВОССТАНОВЛЕНИЯ

После выполнения всех шагов:

```bash
# 1. Проверьте что symlink на месте
ls -la /srv/bunyod-tour/uploads
# Должно показать: uploads -> /var/bunyod-tour/uploads

# 2. Проверьте что файлы доступны
ls -lh /srv/bunyod-tour/uploads/images/

# 3. Проверьте через браузер
# Откройте любой тур/отель в админ-панели
# Фотографии должны отображаться

# 4. Протестируйте загрузку новой фотографии
# Загрузите фото через админку и проверьте что оно сохранилось в:
ls -lh /var/bunyod-tour/uploads/images/
# Новый файл должен появиться
```

---

## 🚀 ШАГ 7: ФИНАЛЬНЫЙ ТЕСТ ДЕПЛОЯ

Протестируйте что update.sh больше не удаляет фотографии:

```bash
# 1. Создайте тестовый файл
touch /var/bunyod-tour/uploads/images/test-file.txt

# 2. Запустите обновление
cd /srv/bunyod-tour
./update.sh

# 3. Проверьте что тестовый файл НЕ удалился
ls -lh /var/bunyod-tour/uploads/images/test-file.txt
# Файл должен быть на месте ✅

# 4. Удалите тестовый файл
rm /var/bunyod-tour/uploads/images/test-file.txt
```

---

## 📊 КРАТКАЯ ТАБЛИЦА ДЕЙСТВИЙ

| Шаг | Действие | Где выполнять | Статус |
|-----|----------|---------------|--------|
| 1 | Восстановить фото из бэкапа Timeweb | Production сервер | ⏳ TODO |
| 2 | Создать `/var/bunyod-tour/uploads` | Production сервер | ⏳ TODO |
| 3 | Создать symlink в `/srv/bunyod-tour/uploads` | Production сервер | ⏳ TODO |
| 4 | Обновить .gitignore (добавить uploads/) | Replit → GitHub | ✅ ГОТОВО |
| 5 | Обновить update.sh (проверка symlink) | Replit → GitHub | ⏳ TODO |
| 6 | Запустить update.sh на production | Production сервер | ⏳ TODO |
| 7 | Проверить что фото отображаются | Браузер | ⏳ TODO |

---

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

### 1. Бэкапы Timeweb
- Проверьте в панели управления Timeweb наличие файловых снапшотов
- Обычно они доступны в разделе "Резервные копии" или "Backups"
- Выберите бэкап **ДО** последнего обновления когда фотографии еще были

### 2. Права доступа
Убедитесь что Node.js процесс (PM2) имеет права на чтение/запись в `/var/bunyod-tour/uploads`:
```bash
sudo chown -R $(whoami):$(whoami) /var/bunyod-tour/uploads
chmod -R 755 /var/bunyod-tour/uploads
```

### 3. Если используете Nginx
Проверьте что Nginx имеет доступ к `/var/bunyod-tour/uploads` для раздачи статики.

### 4. Альтернатива: Object Storage (рекомендуется для будущего)
В проекте уже установлен `@google-cloud/storage`. В будущем рекомендуется мигрировать на облачное хранилище (Google Cloud Storage, AWS S3, или аналоги), чтобы файлы хранились полностью отдельно от сервера приложения.

---

## 🆘 ЕСЛИ ЧТО-ТО ПОШЛО НЕ ТАК

### Проблема: Symlink не создается
```bash
# Проверьте права доступа
ls -la /srv/bunyod-tour
sudo chown -R $(whoami):$(whoami) /srv/bunyod-tour
```

### Проблема: Фотографии не отображаются после восстановления
```bash
# Проверьте что пути в БД совпадают с файловой системой
psql -d bunyod_tour -c "SELECT main_image FROM tours WHERE main_image IS NOT NULL LIMIT 5;"
# Вывод должен быть типа: /uploads/images/tour-123.jpg

# Проверьте что файлы есть
ls -lh /var/bunyod-tour/uploads/images/
```

### Проблема: После update.sh файлы все равно удаляются
```bash
# Убедитесь что /uploads/ в .gitignore на GitHub
# Убедитесь что symlink существует ДО запуска git reset
ls -la /srv/bunyod-tour/uploads
```

---

## 📞 Контрольный список перед завершением

- [ ] Фотографии восстановлены из бэкапа Timeweb
- [ ] Создана постоянная директория `/var/bunyod-tour/uploads`
- [ ] Создан symlink `/srv/bunyod-tour/uploads` → `/var/bunyod-tour/uploads`
- [ ] Обновлен .gitignore (добавлен uploads/)
- [ ] Обновлен update.sh (проверка symlink)
- [ ] Изменения загружены на GitHub
- [ ] Запущен update.sh на production
- [ ] Фотографии отображаются в браузере
- [ ] Протестирована загрузка новых фотографий
- [ ] Протестирован деплой (фото не удаляются)

---

**Дата создания плана**: 17 ноября 2025  
**Автор**: Replit Agent  
**Статус**: Готово к выполнению

**СЛЕДУЮЩИЙ ШАГ**: Выполните Шаг 1 (восстановление из бэкапа) на production сервере.
